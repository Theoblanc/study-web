enum Role {
  REGULAR
  ADMIN
}

type User {
  id: String!
  createdAt: String!
  updatedAt: String!
  email: String!
  password: String!
  role: Role!
}

type UserProfile {
  id: String!
  createdAt: String!
  updatedAt: String!
  email: String!
  # password: String!
  role: Role!
}
